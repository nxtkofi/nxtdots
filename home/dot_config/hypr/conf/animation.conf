# Animations are kept here due to `mainMod shift a` keybinding that toggles between disabled and default animations
source = ~/.config/hypr/conf/animations/omarchy.conf

source = ~/.config/hypr/conf/animation.local.conf
